```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Elevate Business | Marketing & Branding</title>
    
    <!-- Tailwind CSS for styling -->
    <script src="https://cdn.tailwindcss.com"></script>
    
    <!-- Font Awesome for Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;700;900&display=swap" rel="stylesheet">
    
    <!-- Three.js for 3D Background -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>

    <style>
        body {
            margin: 0;
            overflow-x: hidden;
            font-family: 'Outfit', sans-serif;
            background-color: #040b16; /* Глубокий темно-синий фон */
            color: #ffffff;
        }

        /* 3D Canvas Background */
        #bg-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100vw;
            height: 100vh;
            z-index: -1;
            outline: none;
        }

        /* Классы для эффекта матового стекла */
        .glass-panel {
            background: rgba(10, 20, 45, 0.4);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border: 1px solid rgba(100, 180, 255, 0.1);
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.02);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(100, 180, 255, 0.05);
            transition: all 0.4s ease;
        }

        .glass-card:hover {
            background: rgba(14, 165, 233, 0.05); 
            border-color: rgba(14, 165, 233, 0.3);
            transform: translateY(-5px);
        }

        .glass-button {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(100, 180, 255, 0.15);
            transition: all 0.3s ease;
        }

        .glass-button:hover {
            background: rgba(14, 165, 233, 0.15);
            border-color: rgba(14, 165, 233, 0.4);
            transform: translateY(-2px);
        }

        /* Кастомный скроллбар */
        ::-webkit-scrollbar { width: 8px; }
        ::-webkit-scrollbar-track { background: #040b16; }
        ::-webkit-scrollbar-thumb { background: #0ea5e9; border-radius: 4px; }
        ::-webkit-scrollbar-thumb:hover { background: #38bdf8; }

        /* Градиентный текст */
        .text-gradient {
            background: linear-gradient(135deg, #38bdf8, #0ea5e9, #2563eb);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        /* Анимации при скролле */
        .reveal {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s cubic-bezier(0.16, 1, 0.3, 1);
        }
        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body class="antialiased selection:bg-cyan-600 selection:text-white">

    <!-- Контейнер для 3D фона -->
    <canvas id="bg-canvas"></canvas>

    <!-- Навигация -->
    <nav class="fixed w-full top-0 z-50 glass-panel border-b-0 border-white/5 py-4 px-6 md:px-12 flex justify-between items-center">
        <div class="flex items-center gap-3 cursor-pointer" onclick="window.scrollTo(0,0)">
            <div class="w-10 h-10 rounded-full bg-gradient-to-tr from-cyan-400 to-blue-600 flex items-center justify-center shadow-lg shadow-cyan-500/30">
                <i class="fa-solid fa-bolt text-white text-xl"></i>
            </div>
            <span class="text-xl font-bold tracking-wider">Elevate<span class="text-cyan-400">Business</span></span>
        </div>
        <div class="hidden md:flex gap-8 text-sm font-medium text-gray-300">
            <a href="#overview" class="hover:text-white transition-colors">Overview</a>
            <a href="#services" class="hover:text-white transition-colors">Services</a>
            <a href="#photos" class="hover:text-white transition-colors">Photos</a>
            <a href="#reviews" class="hover:text-white transition-colors">Reviews</a>
        </div>
        <a href="tel:041836633" class="hidden md:block px-5 py-2 rounded-full glass-button font-bold text-white text-sm">
            Call Us
        </a>
        <button class="md:hidden text-white text-2xl">
            <i class="fa-solid fa-bars"></i>
        </button>
    </nav>

    <!-- Главная секция -->
    <main id="overview" class="relative z-10 min-h-screen flex flex-col justify-center pt-24 pb-12 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto reveal active">
        <div class="grid grid-cols-1 lg:grid-cols-12 gap-12 items-center">
            
            <!-- Левая часть: Текст -->
            <div class="lg:col-span-7 space-y-8">
                <!-- Динамический статус (Открыто/Закрыто) -->
                <div id="hero-status-pill" class="inline-flex items-center gap-2 px-3 py-1 rounded-full glass-panel border border-green-500/30 text-sm font-medium text-green-300 transition-colors duration-300">
                    <span class="relative flex h-2 w-2">
                      <span id="hero-status-ping" class="animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75"></span>
                      <span id="hero-status-dot" class="relative inline-flex rounded-full h-2 w-2 bg-green-500"></span>
                    </span>
                    <span id="hero-status-text">Загрузка статуса...</span>
                </div>
                
                <h1 class="text-5xl sm:text-6xl md:text-7xl font-black leading-tight tracking-tight">
                    Scale Your Brand with <br>
                    <span class="text-gradient">Elevate Business</span>
                </h1>
                
                <p class="text-lg md:text-xl text-gray-400 max-w-2xl leading-relaxed">
                    We are a premier marketing agency based in Yerevan. From cutting-edge social media marketing to unforgettable brand identity, we turn your vision into market leadership.
                </p>

                <div class="flex flex-wrap gap-4 pt-4">
                    <a href="tel:041836633" class="px-8 py-4 rounded-full bg-gradient-to-r from-cyan-500 to-blue-600 font-bold text-white shadow-lg shadow-cyan-500/30 hover:shadow-cyan-500/50 hover:scale-105 transition-all duration-300">
                        <i class="fa-solid fa-phone mr-2"></i> Call 041 836633
                    </a>
                    <a href="#services" class="px-8 py-4 rounded-full glass-button font-bold text-white">
                        Explore Services
                    </a>
                </div>
            </div>

            <!-- Правая часть: Карточка бизнеса -->
            <div class="lg:col-span-5 w-full">
                <div class="glass-panel rounded-3xl p-6 md:p-8 relative overflow-hidden group">
                    <div class="absolute -top-24 -right-24 w-48 h-48 bg-cyan-500/20 rounded-full blur-3xl group-hover:bg-blue-500/30 transition-colors duration-700"></div>
                    
                    <div class="relative z-10">
                        <div class="flex justify-between items-start mb-6">
                            <div>
                                <h2 class="text-3xl font-bold text-white mb-1">Elevate Business</h2>
                                <p class="text-cyan-400 font-medium">Marketing Agency</p>
                            </div>
                            <div id="card-status-badge" class="flex items-center bg-green-500/20 text-green-400 px-3 py-1 rounded-full text-sm font-bold border border-green-500/30 transition-colors duration-300">
                                Open
                            </div>
                        </div>

                        <!-- Рейтинг -->
                        <a href="#reviews" class="flex items-center gap-3 mb-8 pb-6 border-b border-white/10 group/rating cursor-pointer block w-max">
                            <span class="text-4xl font-black text-white">5.0</span>
                            <div class="flex flex-col">
                                <div class="text-yellow-400 flex gap-1 text-sm">
                                    <i class="fa-solid fa-star"></i>
                                    <i class="fa-solid fa-star"></i>
                                    <i class="fa-solid fa-star"></i>
                                    <i class="fa-solid fa-star"></i>
                                    <i class="fa-solid fa-star"></i>
                                </div>
                                <span class="text-gray-400 text-sm underline decoration-gray-600 mt-1 group-hover/rating:text-white transition-colors">(5) Google Reviews</span>
                            </div>
                        </a>

                        <!-- Кнопки действий -->
                        <div class="grid grid-cols-4 gap-2 mb-8 text-center">
                            <a href="tel:041836633" class="flex flex-col items-center gap-2 group/btn cursor-pointer">
                                <div class="w-12 h-12 rounded-full bg-blue-600 text-white flex items-center justify-center text-lg shadow-lg shadow-blue-500/30 group-hover/btn:scale-110 transition-transform">
                                    <i class="fa-solid fa-phone"></i>
                                </div>
                                <span class="text-xs text-cyan-400 font-medium">Call</span>
                            </a>
                            <div class="flex flex-col items-center gap-2 group/btn cursor-pointer">
                                <div class="w-12 h-12 rounded-full glass-button text-cyan-400 flex items-center justify-center text-lg group-hover/btn:bg-cyan-500/20 group-hover/btn:scale-110 transition-all">
                                    <i class="fa-solid fa-directions"></i>
                                </div>
                                <span class="text-xs text-gray-300">Directions</span>
                            </div>
                            <div class="flex flex-col items-center gap-2 group/btn cursor-pointer">
                                <div class="w-12 h-12 rounded-full glass-button text-cyan-400 flex items-center justify-center text-lg group-hover/btn:bg-cyan-500/20 group-hover/btn:scale-110 transition-all">
                                    <i class="fa-solid fa-share-nodes"></i>
                                </div>
                                <span class="text-xs text-gray-300">Share</span>
                            </div>
                            <div class="flex flex-col items-center gap-2 group/btn cursor-pointer">
                                <div class="w-12 h-12 rounded-full glass-button text-cyan-400 flex items-center justify-center text-lg group-hover/btn:bg-cyan-500/20 group-hover/btn:scale-110 transition-all">
                                    <i class="fa-regular fa-bookmark"></i>
                                </div>
                                <span class="text-xs text-gray-300">Save</span>
                            </div>
                        </div>

                        <!-- Детали -->
                        <div class="space-y-4 text-sm">
                            <div class="flex items-start gap-4">
                                <i class="fa-solid fa-location-dot mt-1 text-gray-400 text-lg w-5 text-center"></i>
                                <div class="text-gray-300 hover:text-white transition-colors cursor-pointer">
                                    <p class="font-medium text-white text-base">51 Tigran Mets Ave</p>
                                    <p>Yerevan 0018, Armenia</p>
                                </div>
                            </div>
                            <!-- Блок времени работы -->
                            <div class="flex items-start gap-4">
                                <i class="fa-regular fa-clock mt-1 text-gray-400 text-lg w-5 text-center"></i>
                                <div>
                                    <p class="text-gray-300 mb-1">
                                        <span id="list-status-text" class="text-white font-bold text-base">Updating...</span>
                                    </p>
                                    <p class="text-gray-400 text-xs">Hours: 09:00 - 21:00 (Mon-Sun)</p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Секция Услуг -->
    <section id="services" class="relative z-10 py-24 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto reveal">
        <div class="text-center mb-16">
            <h2 class="text-4xl md:text-5xl font-black mb-4">Our <span class="text-gradient">Services</span></h2>
            <p class="text-gray-400 max-w-2xl mx-auto">We provide a comprehensive suite of digital services designed to elevate your brand's presence and drive real business growth.</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <!-- Услуга 1 -->
            <div class="glass-card p-8 rounded-2xl">
                <div class="w-14 h-14 rounded-xl bg-cyan-500/20 text-cyan-400 flex items-center justify-center text-2xl mb-6 shadow-[0_0_15px_rgba(34,211,238,0.2)]">
                    <i class="fa-brands fa-instagram"></i>
                </div>
                <h3 class="text-xl font-bold mb-3">Social Media Marketing</h3>
                <p class="text-gray-400 text-sm leading-relaxed">Strategic campaigns across Instagram, Facebook, and TikTok. We build communities, increase engagement, and turn followers into loyal customers.</p>
            </div>
            <!-- Услуга 2 -->
            <div class="glass-card p-8 rounded-2xl">
                <div class="w-14 h-14 rounded-xl bg-blue-500/20 text-blue-400 flex items-center justify-center text-2xl mb-6 shadow-[0_0_15px_rgba(59,130,246,0.2)]">
                    <i class="fa-solid fa-lightbulb"></i>
                </div>
                <h3 class="text-xl font-bold mb-3">Brand Identity & Design</h3>
                <p class="text-gray-400 text-sm leading-relaxed">Logos, color palettes, and visual guidelines. We craft memorable brand identities that resonate with your target audience and stand out in Yerevan.</p>
            </div>
            <!-- Услуга 3 -->
            <div class="glass-card p-8 rounded-2xl">
                <div class="w-14 h-14 rounded-xl bg-sky-500/20 text-sky-400 flex items-center justify-center text-2xl mb-6 shadow-[0_0_15px_rgba(14,165,233,0.2)]">
                    <i class="fa-solid fa-bullseye"></i>
                </div>
                <h3 class="text-xl font-bold mb-3">Targeted Advertising</h3>
                <p class="text-gray-400 text-sm leading-relaxed">Data-driven ad campaigns on Google and Social Media. We optimize your ad spend to ensure maximum ROI and high-quality lead generation.</p>
            </div>
        </div>
    </section>

    <!-- Секция Фото -->
    <section id="photos" class="relative z-10 py-24 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto reveal">
        <div class="flex justify-between items-end mb-12">
            <div>
                <h2 class="text-4xl md:text-5xl font-black mb-4">Latest <span class="text-gradient">Projects</span></h2>
                <p class="text-gray-400">A glimpse into our creative process.</p>
            </div>
            <a href="#" class="hidden sm:inline-flex items-center gap-2 text-cyan-400 hover:text-cyan-300 font-medium transition-colors">
                View all photos <i class="fa-solid fa-arrow-right"></i>
            </a>
        </div>

        <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
            <div class="col-span-2 row-span-2 glass-panel rounded-2xl h-80 relative overflow-hidden group">
                <div class="absolute inset-0 bg-gradient-to-tr from-cyan-600/30 to-blue-800/40 opacity-60 group-hover:scale-110 transition-transform duration-700"></div>
                <div class="absolute bottom-4 left-4">
                    <span class="bg-black/50 backdrop-blur-sm px-3 py-1 rounded-full text-xs font-bold border border-cyan-500/30">Branding</span>
                </div>
            </div>
            <div class="glass-panel rounded-2xl h-36 md:h-auto relative overflow-hidden group">
                <div class="absolute inset-0 bg-gradient-to-br from-sky-500/30 to-indigo-600/40 opacity-60 group-hover:scale-110 transition-transform duration-700"></div>
            </div>
            <div class="glass-panel rounded-2xl h-36 md:h-auto relative overflow-hidden group">
                <div class="absolute inset-0 bg-gradient-to-br from-teal-500/30 to-blue-500/40 opacity-60 group-hover:scale-110 transition-transform duration-700"></div>
            </div>
            <div class="col-span-2 glass-panel rounded-2xl h-40 relative overflow-hidden group">
                <div class="absolute inset-0 bg-gradient-to-r from-blue-600/30 to-cyan-500/40 opacity-60 group-hover:scale-110 transition-transform duration-700"></div>
                <div class="absolute bottom-4 left-4">
                    <span class="bg-black/50 backdrop-blur-sm px-3 py-1 rounded-full text-xs font-bold border border-cyan-500/30">Social Media</span>
                </div>
            </div>
        </div>
        <a href="#" class="sm:hidden mt-6 inline-flex justify-center w-full items-center gap-2 text-cyan-400 hover:text-cyan-300 font-medium transition-colors">
            View all photos <i class="fa-solid fa-arrow-right"></i>
        </a>
    </section>

    <!-- Секция Отзывов -->
    <section id="reviews" class="relative z-10 py-24 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto reveal">
        <div class="flex items-center gap-6 mb-12">
            <h2 class="text-4xl md:text-5xl font-black">Client <span class="text-gradient">Reviews</span></h2>
            <div class="flex bg-white/5 border border-cyan-500/20 rounded-full px-4 py-2 items-center gap-2">
                <span class="text-xl font-bold">5.0</span>
                <div class="text-yellow-400 text-sm">
                    <i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i>
                </div>
            </div>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <!-- Отзыв 1 -->
            <div class="glass-card p-6 rounded-2xl">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-12 h-12 rounded-full bg-gradient-to-br from-cyan-400 to-blue-600 flex items-center justify-center font-bold text-lg shadow-[0_0_10px_rgba(34,211,238,0.3)]">A</div>
                    <div>
                        <h4 class="font-bold">Aram K.</h4>
                        <div class="text-yellow-400 text-xs"><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i></div>
                    </div>
                </div>
                <p class="text-gray-300 text-sm italic">"Elevate Business transformed our social media presence completely. The team in Yerevan is highly professional and their strategies actually brought us new clients within the first month!"</p>
            </div>
            <!-- Отзыв 2 -->
            <div class="glass-card p-6 rounded-2xl">
                <div class="flex items-center gap-4 mb-4">
                    <div class="w-12 h-12 rounded-full bg-gradient-to-br from-sky-400 to-indigo-600 flex items-center justify-center font-bold text-lg shadow-[0_0_10px_rgba(56,189,248,0.3)]">M</div>
                    <div>
                        <h4 class="font-bold">Mariam G.</h4>
                        <div class="text-yellow-400 text-xs"><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i><i class="fa-solid fa-star"></i></div>
                    </div>
                </div>
                <p class="text-gray-300 text-sm italic">"Best marketing agency! They handled our branding from scratch. Highly recommend their services if you want your business to look premium and stand out."</p>
            </div>
        </div>
    </section>

    <!-- Футер -->
    <footer class="relative z-10 border-t border-white/5 mt-12 bg-[#040b16]/80 backdrop-blur-md">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12 flex flex-col md:flex-row justify-between items-center gap-6">
            <div class="flex items-center gap-3">
                <div class="w-8 h-8 rounded-full bg-gradient-to-tr from-cyan-400 to-blue-600 flex items-center justify-center">
                    <i class="fa-solid fa-bolt text-white text-sm"></i>
                </div>
                <span class="text-lg font-bold">Elevate<span class="text-cyan-400">Business</span></span>
            </div>
            <p class="text-gray-500 text-sm text-center md:text-left">© 2024 Elevate Business. 51 Tigran Mets Ave, Yerevan. All rights reserved.</p>
            <div class="flex gap-4">
                <a href="#" class="w-10 h-10 rounded-full glass-button flex items-center justify-center text-cyan-500 hover:text-white hover:bg-cyan-500/20"><i class="fa-brands fa-instagram"></i></a>
                <a href="#" class="w-10 h-10 rounded-full glass-button flex items-center justify-center text-cyan-500 hover:text-white hover:bg-cyan-500/20"><i class="fa-brands fa-facebook-f"></i></a>
                <a href="#" class="w-10 h-10 rounded-full glass-button flex items-center justify-center text-cyan-500 hover:text-white hover:bg-cyan-500/20"><i class="fa-brands fa-linkedin-in"></i></a>
            </div>
        </div>
    </footer>

    <!-- Скрипты (Время, Анимация скролла и 3D Фон) -->
    <script>
        // 1. ЛОГИКА ДИНАМИЧЕСКОГО ВРЕМЕНИ (ПО ЕРЕВАНУ)
        function updateBusinessStatus() {
            const options = { timeZone: 'Asia/Yerevan', hour: '2-digit', minute: '2-digit', hour12: false };
            const formatter = new Intl.DateTimeFormat([], options);
            
            const timeString = formatter.format(new Date());
            const currentHour = parseInt(timeString.split(':')[0], 10);
            
            // Агентство работает с 09:00 до 21:00 (9 AM - 9 PM)
            const isOpen = currentHour >= 9 && currentHour < 21;

            const pillStatus = document.getElementById('hero-status-pill');
            const pillText = document.getElementById('hero-status-text');
            const pillPing = document.getElementById('hero-status-ping');
            const pillDot = document.getElementById('hero-status-dot');
            
            const cardBadge = document.getElementById('card-status-badge');
            const listText = document.getElementById('list-status-text');

            if (isOpen) {
                pillStatus.className = "inline-flex items-center gap-2 px-3 py-1 rounded-full glass-panel border border-green-500/30 text-sm font-medium text-green-300 transition-colors duration-300";
                pillText.textContent = "Open Now · Closes 9 PM";
                pillPing.className = "animate-ping absolute inline-flex h-full w-full rounded-full bg-green-400 opacity-75";
                pillDot.className = "relative inline-flex rounded-full h-2 w-2 bg-green-500";
                
                cardBadge.className = "flex items-center bg-green-500/20 text-green-400 px-3 py-1 rounded-full text-sm font-bold border border-green-500/30 transition-colors duration-300";
                cardBadge.textContent = "Open";
                
                listText.textContent = "Open · Closes at 21:00";
                listText.className = "text-green-400 font-bold text-base";
            } else {
                pillStatus.className = "inline-flex items-center gap-2 px-3 py-1 rounded-full glass-panel border border-red-500/30 text-sm font-medium text-red-300 transition-colors duration-300";
                pillText.textContent = "Closed · Opens 9 AM";
                pillPing.className = "animate-ping absolute inline-flex h-full w-full rounded-full bg-red-400 opacity-75";
                pillDot.className = "relative inline-flex rounded-full h-2 w-2 bg-red-500";
                
                cardBadge.className = "flex items-center bg-red-500/20 text-red-400 px-3 py-1 rounded-full text-sm font-bold border border-red-500/30 transition-colors duration-300";
                cardBadge.textContent = "Closed";
                
                listText.textContent = "Closed · Opens at 09:00";
                listText.className = "text-red-400 font-bold text-base";
            }
        }

        // 2. АНИМАЦИЯ ПОЯВЛЕНИЯ ПРИ СКРОЛЛЕ
        function initScrollAnimations() {
            const reveals = document.querySelectorAll('.reveal');
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('active');
                    }
                });
            }, { threshold: 0.1 });

            reveals.forEach(reveal => observer.observe(reveal));
        }

        // 3. THREE.JS 3D ФОН (Темно-синяя тема)
        window.onload = function () {
            // Запуск логики времени
            updateBusinessStatus();
            setInterval(updateBusinessStatus, 60000);
            initScrollAnimations();

            let scene, camera, renderer, particleSystem;
            
            let mouseX = 0;
            let mouseY = 0;
            let targetX = 0;
            let targetY = 0;
            const windowHalfX = window.innerWidth / 2;
            const windowHalfY = window.innerHeight / 2;

            function init3D() {
                scene = new THREE.Scene();
                scene.fog = new THREE.FogExp2(0x040b16, 0.001);

                camera = new THREE.PerspectiveCamera(75, window.innerWidth / window.innerHeight, 1, 3000);
                camera.position.z = 1000;

                const canvas = document.getElementById('bg-canvas');
                renderer = new THREE.WebGLRenderer({ canvas: canvas, alpha: true, antialias: true });
                renderer.setPixelRatio(window.devicePixelRatio);
                renderer.setSize(window.innerWidth, window.innerHeight);

                const geometry = new THREE.BufferGeometry();
                const particlesCount = 1500;
                
                const positions = new Float32Array(particlesCount * 3);
                const colors = new Float32Array(particlesCount * 3);
                
                // Цвета
                const color1 = new THREE.Color(0x00f2fe); 
                const color2 = new THREE.Color(0x0ea5e9); 
                const color3 = new THREE.Color(0x2563eb); 

                for (let i = 0; i < particlesCount; i++) {
                    const radius = 1200 * Math.cbrt(Math.random());
                    const theta = Math.random() * 2 * Math.PI;
                    const phi = Math.acos(2 * Math.random() - 1);
                    
                    const x = radius * Math.sin(phi) * Math.cos(theta);
                    const y = radius * Math.sin(phi) * Math.sin(theta);
                    const z = radius * Math.cos(phi);

                    positions[i * 3] = x;
                    positions[i * 3 + 1] = y;
                    positions[i * 3 + 2] = z;

                    const mixedColor = color1.clone();
                    const mixRatio = Math.random();
                    if (mixRatio < 0.5) {
                        mixedColor.lerp(color2, mixRatio * 2);
                    } else {
                        mixedColor.lerp(color3, (mixRatio - 0.5) * 2);
                    }

                    colors[i * 3] = mixedColor.r;
                    colors[i * 3 + 1] = mixedColor.g;
                    colors[i * 3 + 2] = mixedColor.b;
                }

                geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
                geometry.setAttribute('color', new THREE.BufferAttribute(colors, 3));

                // Создание текстуры частицы
                const canvasTexture = document.createElement('canvas');
                canvasTexture.width = 32;
                canvasTexture.height = 32;
                const context = canvasTexture.getContext('2d');
                const gradient = context.createRadialGradient(16, 16, 0, 16, 16, 16);
                gradient.addColorStop(0, 'rgba(255,255,255,1)');
                gradient.addColorStop(0.2, 'rgba(255,255,255,0.8)');
                gradient.addColorStop(0.5, 'rgba(255,255,255,0.2)');
                gradient.addColorStop(1, 'rgba(0,0,0,0)');
                context.fillStyle = gradient;
                context.fillRect(0, 0, 32, 32);
                const sprite = new THREE.CanvasTexture(canvasTexture);

                const material = new THREE.PointsMaterial({
                    size: 8,
                    vertexColors: true,
                    map: sprite,
                    blending: THREE.AdditiveBlending,
                    depthWrite: false,
                    transparent: true,
                    opacity: 0.8
                });

                particleSystem = new THREE.Points(geometry, material);
                scene.add(particleSystem);

                // Геометрические ядра
                const coreGeometry = new THREE.IcosahedronGeometry(300, 1);
                const coreMaterial = new THREE.MeshBasicMaterial({
                    color: 0x0ea5e9,
                    wireframe: true,
                    transparent: true,
                    opacity: 0.05
                });
                const coreMesh = new THREE.Mesh(coreGeometry, coreMaterial);
                scene.add(coreMesh);
                
                const coreMesh2 = new THREE.Mesh(
                    new THREE.IcosahedronGeometry(400, 2),
                    new THREE.MeshBasicMaterial({ color: 0x38bdf8, wireframe: true, transparent: true, opacity: 0.03 })
                );
                scene.add(coreMesh2);

                document.addEventListener('mousemove', onDocumentMouseMove, false);
                document.addEventListener('touchstart', onDocumentTouchStart, { passive: false });
                document.addEventListener('touchmove', onDocumentTouchMove, { passive: false });
                window.addEventListener('resize', onWindowResize, false);
            }

            function onWindowResize() {
                camera.aspect = window.innerWidth / window.innerHeight;
                camera.updateProjectionMatrix();
                renderer.setSize(window.innerWidth, window.innerHeight);
            }

            function onDocumentMouseMove(event) {
                mouseX = (event.clientX - windowHalfX) * 0.5;
                mouseY = (event.clientY - windowHalfY) * 0.5;
            }

            function onDocumentTouchStart(event) {
                if (event.touches.length === 1) {
                    mouseX = (event.touches[0].pageX - windowHalfX) * 0.5;
                    mouseY = (event.touches[0].pageY - windowHalfY) * 0.5;
                }
            }

            function onDocumentTouchMove(event) {
                if (event.touches.length === 1) {
                    mouseX = (event.touches[0].pageX - windowHalfX) * 0.5;
                    mouseY = (event.touches[0].pageY - windowHalfY) * 0.5;
                }
            }

            function animate() {
                requestAnimationFrame(animate);
                render();
            }

            function render() {
                targetX = mouseX * 0.5;
                targetY = mouseY * 0.5;
                
                camera.position.x += (targetX - camera.position.x) * 0.02;
                camera.position.y += (-targetY - camera.position.y) * 0.02;
                camera.lookAt(scene.position);

                const time = Date.now() * 0.00005;
                if (particleSystem) {
                    particleSystem.rotation.y = time;
                    particleSystem.rotation.x = time * 0.5;
                }
                
                scene.children.forEach(child => {
                    if (child.type === "Mesh") {
                        child.rotation.x -= 0.001;
                        child.rotation.y += 0.002;
                    }
                });

                renderer.render(scene, camera);
            }

            init3D();
            animate();
        };
    </script>
</body>
</html>


```
